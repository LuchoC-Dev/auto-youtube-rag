Closes #

## Summary

<!-- What changed and why. -->

## Verification

<!-- Check off what you ran locally and note the result (pass/fail, with
     detail on any failure). -->

- [ ] `npm run check` (typecheck + lint + test + format:check)
- [ ] `npm run build`
- [ ] `npm run test:embedding:smoke` (only if this touches embeddings or
      model installation)
- [ ] `npm run test:install:smoke` (only if this touches install/setup)

## Docs

- [ ] Updated relevant docs (`README.md`, `docs/`, `skill/`) if this change
      affects behavior, commands, or invariants.
